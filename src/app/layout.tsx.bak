import type { Metadata, Viewport } from 'next';
import './globals.css';
import { SyncStatusBar } from '@/components/SyncStatusBar';

export const metadata: Metadata = {
  title: 'EgBE Memory Engine',
  description: 'Spaced Repetition & Clinical Scenarios for Mahidol BME',
  manifest: '/manifest.json',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'black-translucent',
    title: 'EgBE',
  },
};

export const viewport: Viewport = {
  themeColor: '#1a1a2e',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="antialiased bg-zinc-950 text-zinc-100 min-h-screen flex flex-col">
        <header className="border-b border-zinc-800 bg-zinc-900/80 backdrop-blur sticky top-0 z-50">
          <nav className="max-w-6xl mx-auto px-4 h-14 flex items-center gap-6 text-sm">
            <a href="/" className="font-bold text-teal-400 text-base tracking-tight">
              EgBE
            </a>
            <a href="/curriculum" className="text-zinc-400 hover:text-zinc-200 transition-colors">
              Curriculum
            </a>
            <a href="/study" className="text-zinc-400 hover:text-zinc-200 transition-colors">
              Study
            </a>
            <a href="/scenarios" className="text-zinc-400 hover:text-zinc-200 transition-colors">
              Scenarios
            </a>
            <a href="/graph" className="text-zinc-400 hover:text-zinc-200 transition-colors">
              Graph
            </a>
            <div className="flex-1" />
            <SyncStatusBar />
          </nav>
        </header>
        <main className="flex-1">{children}</main>
        <footer className="border-t border-zinc-800 py-4 text-center text-xs text-zinc-600">
          EgBE Memory Engine · Mahidol University · Biomedical Engineering
        </footer>
      </body>
    </html>
  );
}